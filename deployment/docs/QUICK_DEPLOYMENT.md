# 🚀 دليل سريع لنشر التطبيق - مشروع التخرج

## ✅ هل المشروع كامل؟

نعم! المشروع **كامل 100%** ويشمل:
- ✅ تطبيق محمول متكامل (React Native + Expo)
- ✅ Backend Server (Node.js + Express + MongoDB)
- ✅ لوحة إدارة (Next.js)
- ✅ جميع الميزات المطلوبة
- ✅ نظام إشعارات
- ✅ نظام مكتبة رقمية
- ✅ نظام حضور
- ✅ نظام أقساط
- ✅ نظام أخبار وفعاليات

---

## 📱 خطوات إنشاء باركود للتحميل التلقائي

### الطريقة 1: استخدام Expo EAS Build (موصى بها)

#### 1. تثبيت EAS CLI
```bash
npm install -g eas-cli
```

#### 2. تسجيل الدخول
```bash
eas login
```

#### 3. تهيئة المشروع
```bash
eas build:configure
```

#### 4. بناء التطبيق

**لـ Android:**
```bash
npm run build:android
# أو
eas build --platform android --profile preview
```

**لـ iOS:**
```bash
npm run build:ios
# أو
eas build --platform ios --profile preview
```

**لـ كلا المنصتين:**
```bash
npm run build:all
# أو
eas build --platform all --profile preview
```

#### 5. الحصول على رابط التحميل

بعد اكتمال البناء، ستحصل على:
- رابط مباشر للتحميل
- QR Code للتحميل التلقائي

#### 6. إنشاء صفحة HTML مع باركود

افتح ملف `public/download.html` في المتصفح وأضف رابط التحميل:
```
http://your-domain.com/download.html?url=YOUR_DOWNLOAD_URL&ios=IOS_URL&android=ANDROID_URL
```

---

### الطريقة 2: استخدام Expo Go (للاختبار السريع)

```bash
# تشغيل التطبيق
npm run dev

# ستحصل على QR Code في Terminal
# امسحه باستخدام Expo Go app
```

**ملاحظة**: Expo Go محدود ولا يدعم جميع الميزات (مثل Face ID، Camera، إلخ)

---

## 🔐 إعداد حساب تجريبي

### إنشاء حساب تجريبي في قاعدة البيانات:

```bash
cd server
npm run create:demo
```

**بيانات الحساب التجريبي:**
- **Email**: `demo@alhikma.edu`
- **Password**: `demo123`
- **القسم**: هندسة تقنيات الحاسوب
- **المرحلة**: 3

---

## 📋 خطوات النشر الكاملة

### 1. إعداد المشروع

```bash
# تحديث app.json (تم بالفعل)
# تحديث eas.json (تم بالفعل)
```

### 2. بناء التطبيق

```bash
# Android
npm run build:android

# iOS
npm run build:ios

# أو كليهما
npm run build:all
```

### 3. رفع التطبيق على المتاجر

#### Android (Google Play):
```bash
npm run submit:android
```

#### iOS (App Store):
```bash
npm run submit:ios
```

### 4. إنشاء صفحة التحميل

1. ارفع ملف `public/download.html` على خادم ويب
2. أضف روابط التحميل في URL parameters
3. اطبع الباركود أو اعرضه على شاشة

---

## 🎯 للعرض في مشروع التخرج

### الخيار 1: استخدام EAS Build (موصى به)

1. **بناء التطبيق:**
   ```bash
   eas build --platform all --profile preview
   ```

2. **الحصول على روابط التحميل:**
   - بعد اكتمال البناء، ستحصل على روابط مباشرة
   - استخدم هذه الروابط في صفحة HTML

3. **إنشاء QR Code:**
   - افتح `public/download.html`
   - أضف روابط التحميل
   - اطبع الصفحة أو اعرضها على شاشة

### الخيار 2: استخدام Expo Development Build

1. **بناء Development Build:**
   ```bash
   eas build --profile development --platform all
   ```

2. **تثبيت على الأجهزة:**
   - Android: APK
   - iOS: IPA (يتطلب TestFlight أو تثبيت مباشر)

---

## 📱 معلومات مهمة

### Android:
- ✅ يمكن تثبيت APK مباشرة
- ✅ لا يتطلب متجر
- ✅ أسهل للتوزيع

### iOS:
- ⚠️ يتطلب حساب Apple Developer ($99/سنة)
- ⚠️ يمكن استخدام TestFlight (مجاني)
- ⚠️ أو تثبيت مباشر (يتطلب توقيع)

---

## 🎨 صفحة التحميل

تم إنشاء صفحة HTML جاهزة في `public/download.html` تحتوي على:
- ✅ QR Code للتحميل
- ✅ روابط مباشرة لـ iOS و Android
- ✅ تعليمات التثبيت
- ✅ بيانات الحساب التجريبي

**للاستخدام:**
1. ارفع الملف على خادم ويب
2. أضف روابط التحميل في URL:
   ```
   download.html?url=DOWNLOAD_URL&ios=IOS_URL&android=ANDROID_URL
   ```

---

## 📞 الدعم

للمزيد من المعلومات:
- [EAS Build Documentation](https://docs.expo.dev/build/introduction/)
- [Expo Deployment Guide](https://docs.expo.dev/distribution/introduction/)

---

## ✅ قائمة التحقق النهائية

- [ ] تحديث `app.json` بمعلومات التطبيق
- [ ] بناء التطبيق لـ Android
- [ ] بناء التطبيق لـ iOS
- [ ] اختبار التطبيق على أجهزة حقيقية
- [ ] إنشاء حساب تجريبي
- [ ] إعداد صفحة التحميل
- [ ] طباعة QR Code
- [ ] إعداد العرض

---

**مبروك! مشروعك جاهز للعرض 🎉**

