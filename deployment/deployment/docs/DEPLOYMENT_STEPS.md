# 📱 خطوات نشر التطبيق - دليل شامل

## ✅ هل المشروع كامل؟

**نعم! المشروع كامل 100%** ويشمل جميع الميزات المطلوبة لمشروع التخرج.

---

## 🎯 الهدف: إنشاء باركود للتحميل التلقائي

### الخيار 1: استخدام Expo EAS Build (الأفضل للعرض)

#### الخطوة 1: تثبيت EAS CLI
```bash
npm install -g eas-cli
```

#### الخطوة 2: تسجيل الدخول
```bash
eas login
```

#### الخطوة 3: تهيئة المشروع
```bash
eas build:configure
```

#### الخطوة 4: بناء التطبيق

**لـ Android (APK - أسهل للتوزيع):**
```bash
npm run build:android
```

**لـ iOS (يتطلب حساب Apple Developer):**
```bash
npm run build:ios
```

**لـ كلا المنصتين:**
```bash
npm run build:all
```

#### الخطوة 5: الحصول على رابط التحميل

بعد اكتمال البناء (قد يستغرق 10-20 دقيقة):
1. ستحصل على رابط مباشر للتحميل
2. انسخ الرابط

#### الخطوة 6: إنشاء صفحة HTML مع QR Code

```bash
# تحديث روابط التحميل في scripts/generateDownloadPage.js
# ثم شغّل:
npm run generate:download-page
```

أو افتح `public/download.html` في المتصفح وأضف الرابط.

---

### الخيار 2: استخدام Expo Development Build (للاختبار)

```bash
# بناء Development Build
eas build --profile development --platform all

# بعد اكتمال البناء، ستحصل على:
# - APK لـ Android
# - IPA لـ iOS (يتطلب TestFlight)
```

---

## 🔐 إعداد حساب تجريبي

```bash
cd server
npm run create:demo
```

**بيانات الحساب:**
- Email: `demo@alhikma.edu`
- Password: `demo123`

---

## 📋 خطوات سريعة للعرض

### 1. إعداد البيانات
```bash
# في مجلد server
npm run seed          # البيانات الأساسية
npm run seed:real     # بيانات حقيقية (كتب، إشعارات، أخبار)
npm run create:demo   # حساب تجريبي
```

### 2. بناء التطبيق
```bash
# في المجلد الرئيسي
npm run build:android  # لـ Android
npm run build:ios      # لـ iOS
```

### 3. إنشاء صفحة التحميل
```bash
# بعد الحصول على رابط التحميل
npm run generate:download-page
```

### 4. عرض الباركود
- افتح `public/download.html` في المتصفح
- اطبع الصفحة أو اعرضها على شاشة
- امسح الباركود بالكاميرا

---

## 📱 معلومات مهمة

### Android:
- ✅ **APK**: يمكن تثبيته مباشرة بدون متجر
- ✅ **سهل التوزيع**: أرسل الملف أو ارفعه على Google Drive
- ✅ **لا يتطلب حساب**: مجاني تماماً

### iOS:
- ⚠️ **يتطلب حساب Apple Developer**: $99/سنة
- ✅ **TestFlight**: مجاني للتوزيع (حتى 10,000 مستخدم)
- ⚠️ **تثبيت مباشر**: يتطلب توقيع وتثبيت يدوي

---

## 🎨 صفحة التحميل الجاهزة

تم إنشاء صفحة HTML في `public/download.html` تحتوي على:
- ✅ QR Code للتحميل
- ✅ روابط مباشرة
- ✅ تعليمات التثبيت
- ✅ بيانات الحساب التجريبي

---

## 🚀 للعرض في مشروع التخرج

### الطريقة الموصى بها:

1. **بناء APK لـ Android:**
   ```bash
   npm run build:android
   ```

2. **الحصول على رابط التحميل:**
   - بعد اكتمال البناء، انسخ الرابط

3. **إنشاء صفحة HTML:**
   ```bash
   # عدّل scripts/generateDownloadPage.js وأضف الرابط
   npm run generate:download-page
   ```

4. **عرض الباركود:**
   - افتح `public/download.html`
   - اطبع أو اعرض على شاشة
   - امسح الباركود لتحميل التطبيق

---

## 📞 ملاحظات مهمة

1. **لـ iOS**: يتطلب حساب Apple Developer ($99/سنة)
2. **لـ Android**: مجاني تماماً
3. **للاختبار السريع**: استخدم Expo Go (محدود)
4. **للعرض الاحترافي**: استخدم EAS Build

---

## ✅ قائمة التحقق

- [ ] تحديث `app.json`
- [ ] بناء التطبيق
- [ ] اختبار على أجهزة حقيقية
- [ ] إنشاء حساب تجريبي
- [ ] إنشاء صفحة التحميل
- [ ] طباعة QR Code
- [ ] إعداد العرض

---

**مبروك! مشروعك جاهز للعرض 🎉**

