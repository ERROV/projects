# إعداد API URL

## المشكلة
إذا ظهر خطأ `Network request failed` عند محاولة تسجيل الدخول، فهذا يعني أن التطبيق لا يستطيع الاتصال بالخادم.

## الحل

### 1. تأكد من تشغيل الخادم
```bash
cd server
npm start
```

يجب أن ترى رسالة مثل:
```
Server running on port 5000
MongoDB Connected
```

### 2. حدد عنوان IP الصحيح

#### على Windows:
```powershell
ipconfig
```
ابحث عن `IPv4 Address` تحت `Wireless LAN adapter Wi-Fi` أو `Ethernet adapter`

#### على Mac/Linux:
```bash
ifconfig
# أو
ip addr show
```

### 3. قم بتحديث API URL

#### الطريقة الأولى: في `app.json` (موصى بها)
افتح `app.json` وعدّل:
```json
{
  "expo": {
    "extra": {
      "apiUrl": "http://YOUR_IP_ADDRESS:5000/api"
    }
  }
}
```

مثال:
```json
{
  "expo": {
    "extra": {
      "apiUrl": "http://192.168.1.100:5000/api"
    }
  }
}
```

#### الطريقة الثانية: إنشاء ملف `.env`
أنشئ ملف `.env` في المجلد الرئيسي:
```
EXPO_PUBLIC_API_URL=http://YOUR_IP_ADDRESS:5000/api
```

### 4. أعد تشغيل التطبيق
```bash
# أوقف التطبيق (Ctrl+C)
# ثم أعد تشغيله
npm run dev
```

### 5. تحقق من الاتصال
عند تشغيل التطبيق، ستظهر في الـ console:
```
🔗 API URL: http://YOUR_IP_ADDRESS:5000/api
```

## ملاحظات مهمة

1. **لا تستخدم `localhost`** عند الاختبار على جهاز حقيقي - استخدم IP جهازك
2. **تأكد من أن الخادم والجهاز على نفس الشبكة**
3. **تحقق من Firewall** - قد يحتاج المنفذ 5000 إلى فتحه
4. **عند استخدام Android Emulator**: استخدم `10.0.2.2` بدلاً من `localhost`

## استكشاف الأخطاء

### الخطأ: "Network request failed"
- تأكد من تشغيل الخادم
- تحقق من IP Address
- تأكد من أن الخادم والجهاز على نفس الشبكة

### الخطأ: "Connection refused"
- تحقق من أن الخادم يعمل على المنفذ 5000
- تحقق من Firewall

### الخطأ: "Timeout"
- تأكد من أن IP Address صحيح
- تحقق من اتصال الشبكة

